<!-- <%= @user.name %>, <%= @user.email %> -->
<!-- pokazuje obrazek oraz nazwe usera -->

<% provide(:title, @user.name) %> <!-- brak tej lini kodu nic nie zmienia -->


<!-- zmienna user powstaje w kontrolerze
u nas jest to: "app/controllers/users_controller.rb"
gdzie zdefiniowalismy metode "show":

def show
    @user = User.find(params[:id])
end

pytanie skad sie bierze wartosc "id"? Wydaje mi sie, ze jest to po prostu wartosc sczytana z adresu podanego w przegladarce
 -->

<h1>
  <%= gravatar_for @user %> <!-- "gravatar_for" nie jest zdefiniowane, definiujemy wiec te metode w "app/helpers/users_helper.rb" -->
  <%= @user.name %>
  <%= @user.email %>
</h1>
